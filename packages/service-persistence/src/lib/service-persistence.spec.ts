import { servicePersistence } from './service-persistence.js';

describe('servicePersistence', () => {
  it('should work', () => {
    expect(servicePersistence()).toEqual('service-persistence');
  });
});
